# compiled binary + assembled bundle (build.sh regenerates both)
KeepAwake
KeepAwake.app/

# Python
__pycache__/
*.pyc
.venv/

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Xcode / swiftc intermediates (if ever added)
DerivedData/
*.o
*.swp
